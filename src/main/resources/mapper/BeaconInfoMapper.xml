<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lgguan.iot.position.mapper.IBeaconInfoMapper">


    <select id="getBoundNameByDeviceId" resultType="string">
        select name from personnel_info pi where tag = #{deviceId}
        union all
        select name from thing_info ti where tag = #{deviceId}
        limit 1
    </select>
</mapper>
